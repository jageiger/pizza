<%= form_for(@applied_mod_group) do |f| %>
  <% if @applied_mod_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@applied_mod_group.errors.count, "error") %> prohibited this applied_mod_group from being saved:</h2>

      <ul>
      <% @applied_mod_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :modifier_group_id %><br>
    <%= f.select :modifier_group_id, options_from_collection_for_select(@modifier_groups, "id", "name", @applied_mod_group.modifier_group_id), {}, class: "form-control", :prompt => "Select modifier group" %>
  </div>
  <div class="field">
    <%= f.label :item_id %><br>
    <%= f.select :item_id, options_from_collection_for_select(@items, "id", "name", @applied_mod_group.item_id), {}, class: "form-control", :prompt => "Select item" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
