<%= form_for(@modifier_group) do |f| %>
  <% if @modifier_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@modifier_group.errors.count, "error") %> prohibited this modifier_group from being saved:</h2>

      <ul>
      <% @modifier_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
